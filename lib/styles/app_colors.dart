import 'package:flutter/material.dart';

class AppColors {
  // 🌞 Light Theme
  static const lightBackground = Color(0xFFFDF3F7);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightPrimary = Color(0xFFFF69B4);
  static const lightAccent = Color(0xFF696468);
  static const lightTextPrimary = Color(0xFF212121);
  static const lightTextSecondary = Color(0xFF757575);
  static const lightInputFill = Color(0xFFF5F5F5); // ✅ instead of grey[100]
  static const lightDivider = Color(0xFFE0E0E0);

  // 🌚 Dark Theme
  static const darkBackground = Color(0xFF383138);
  static const darkCard = Color(0xFF2D2A2D);
  static const darkPrimary = Color(0xFFFF8EC8);
  static const darkAccent = Color(0xFF696468);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB0B0B0);
  static const darkInputFill = Color(0xFF444044); // ✅ instead of grey
  static const darkDivider = Color(0xFF555555);

  // Common brand
  static const appleBlack = Color(0xFF000000);
  static const googleBorder = Color(0xFFDDDDDD);
}

